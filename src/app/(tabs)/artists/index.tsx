import { Text,View } from 'react-native'
import { defaultStyle } from '@/styles'

const ArtistScreen = () => {
    return (
        <View style={defaultStyle.container}>
            <Text style={defaultStyle.text}>Artist screen</Text>
        </View>
    )
}

export default ArtistScreen