import { Text,View } from 'react-native'
import { defaultStyle } from '@/styles'

const FavoriteScreen = () => {
    return (
        <View style={defaultStyle.container}>
            <Text style={defaultStyle.text}>Favorite screen</Text>
        </View>
    )
}

export default FavoriteScreen