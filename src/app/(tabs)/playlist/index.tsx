import { Text,View } from 'react-native'
import { defaultStyle } from '@/styles'

const PlaylistScreen = () => {
    return (
        <View style={defaultStyle.container}>
            <Text style={defaultStyle.text}>Playlist screen</Text>
        </View>
    )
}

export default PlaylistScreen